{
    "name": "wiki-quiz-backend",
    "version": "1.0.0",
    "description": "Wiki Quiz App - FastAPI Backend",
    "scripts": {
        "install-deps": "pip install -r requirements.txt",
        "start": "venv\\Scripts\\uvicorn.exe app.main:app --reload --host 0.0.0.0 --port 8000"
    }
}